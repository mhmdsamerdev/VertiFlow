// Static FARMS data has been removed.
// All farm/zone data is now loaded dynamically from the backend API via ZoneContext.
// See: src/context/ZoneContext.tsx  →  GET /config/farms + GET /config/zones
export {}
